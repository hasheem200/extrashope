1 file changed: public/website-settings.html

Made the "Upload Preset" field bulletproof against display issues:
- Added explicit type="text"
- Added inline styles forcing visible black text on a white
  background (color, background, and -webkit-text-fill-color all
  set explicitly) so no page-wide CSS rule (dark mode, autofill
  styling, etc.) can make the typed text invisible.
- Everything else (id, save/load logic, backend) was already
  re-verified working in the previous round — full round-trip
  tested again just now (save -> database -> load back) and it
  passes.

If the field STILL doesn't show what you type after this, it's
worth checking in the browser:
1. Hard refresh the page (Ctrl+Shift+R) to rule out cached CSS/JS
   from before this fix.
2. Right-click the field -> Inspect -> check the Styles panel for
   any rule still overriding color (a browser extension or
   something outside this file could theoretically still interfere,
   though that would be unusual).
